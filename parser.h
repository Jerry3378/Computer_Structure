#ifndef PARSER_H
#define PARSER_H

/*
 * Function: parse
 * ----------------------------
 *   Parses the given text line based on .txt files syntax.
 * 
 *   text_line: The text line to be parsed.
 * 
 *   Returns: Returns 1 if parsing is successful, otherwise exits the program with an error message.
 */

int parse(char *text_line);

#endif 